/**
 * Crear clase main para probar todos los siguientes métodos:
 * 
 * 	1-Método que devuelva la suma de los elementos de un VectorInt.
 * 	2-Método que calcule y escriba la suma de los elementos de un VectorInt.
 * 	3-Método que cuente el nº de pares de un VectorInt.
 * 	4-Método que cuente el nº de impares de un VectorInt.
 * 	5-Método que encuentre y devuelva el mayor de los elementos.
 * 	6-Método que encuentre y devuelva el menor de los elementos.
 * 	7-Método que dado un nº entero indique si existe o no en el vector y 
 * 	  la posición en caso de encontrarlo.
 * 	8-Método que dado un valor devuelva el nº de veces que se repite y 
 * 	  en qué posiciones se ha encontrado.
 * 
 * NOTA: Tomar como referencia la clase Vector de este mismo paquete.
 */
package Arrays_PRUEBAS_CLASE;

public class Vectores2 {

}
